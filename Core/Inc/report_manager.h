/**
 * @file    report_manager.h
 * @brief   UART1 数据上报管理 — 支持 TEXT / JSON / HEX 三种格式
 * @version 2.4 — 低功耗改用 Standby 模式
 */
#ifndef __REPORT_MANAGER_H
#define __REPORT_MANAGER_H

#include "sys_config.h"

/* ═══════════════════════════════════════════════════════════════════════════
 *  上报处理 (在 main while(1) 中调用, 内部按各从机轮询周期判断)
 * ═══════════════════════════════════════════════════════════════════════════ */
void REPORT_Process(void);

/* ═══════════════════════════════════════════════════════════════════════════
 *  格式化单个从机数据 → 缓冲区
 * ═══════════════════════════════════════════════════════════════════════════ */
uint16_t REPORT_Format_Slave(uint8_t slave_idx, char *buf, uint16_t buf_size);

/* ═══════════════════════════════════════════════════════════════════════════
 *  UART1 发送
 * ═══════════════════════════════════════════════════════════════════════════ */
void REPORT_Send(const char *data, uint16_t len);

#endif /* __REPORT_MANAGER_H */
